# Work 协议修复记录（2026-09-18）

本轮修复已于 2026-09-18 09:23:54（Asia/Shanghai）发布到 `107.175.76.239:18082` 的 `sub2api-pool-bald-app`。按用户要求，限流、429 冷却、工具帧策略和重试策略保持原样。工作区原有修改已保留。

## 已修复

| 项目 | 修改后的行为 | 验证 |
|---|---|---|
| turn 与压缩窗口 | 保留合法入站 `turn_id`、开始时间、窗口编号和 `context_window_id`；窗口头与 body 一致。缺少 context ID 时按会话和窗口编号派生 | HTTP 工具续接、窗口 0→1→2、非法值回退 |
| 固定会话 | 固定值作为账号命名空间，Responses 请求继续按账号、API Key、任务隔离；缺少任务标识时不合并到一个账号公共会话 | 不同任务/API Key/账号隔离；同一任务复用 |
| WS metadata | 每帧 metadata 优先于握手中的旧 metadata；原生连接池、透传和 HTTP 回退均同步当前窗口。压缩请求保持 compaction 类型，预热不带正式 turn/root ID | 五模型 × 普通 turn/压缩/预热，均使用 high；两个原生 WS 模式各连续三帧；预热后正式帧不受污染 |
| HTTP Cookie | 模型网关按账号、代理、ChatGPT 身份和导入 Cookie 隔离内存 CookieJar，接收上游 `Set-Cookie`，尊重过期、删除、域及路径；不接收下游 Cookie，不带给遥测端点 | 无初始 Cookie、轮换、删除、跨账号/代理、外域响应和实际请求入口 |
| 额度查询 Cookie 隔离 | WebView 客户端缓存增加账号与 ChatGPT 身份维度，同一代理下的不同账号不再共用 CookieJar | 保持同账号复用，跨账号、身份替换、代理切换隔离 |
| 代理缺失 | 已指定代理却无法得到地址时，管理 API、额度查询和模型 HTTP/WS 入口返回错误，不退回直连 | 缺失仓库、查询失败、无记录及未发生客户端调用 |
| 工具历史 | 有 `previous_response_id` 的增量输出保留原始 `call_id`，避免仅改写输出而与服务端历史脱节；新增转换前/后配对计数 | function/custom/search 输出保留；800 项合成工具历史在 metadata 改写前后字节一致 |
| 完成后的状态保存 | response→account 的 Redis 写入脱离已取消的下游 context，仍保留原有 3 秒超时和进程内映射 | 取消后完成写入，新建本地 store 从共享缓存读回 |
| Lite 图片参数 | 消除布尔短路，前面的字段已变更时仍移除需要剥离的图片 detail 参数 | 对应既有失败已通过 |
| 遥测可观察性 | 本地日志记录批次账号、是否用代理、HTTP 状态、耗时和结果；识别 OTLP 2xx 中的 `partialSuccess.rejectedDataPoints` | 202、两种 partialSuccess 编码、拒绝响应及日志脱敏；不改变重试行为 |
| TLS 画像 | 0.155 初始握手的 supported groups 与 key shares 均改为混合密钥交换优先 | 直接捕获隔离的官方二进制；Go 实际 ClientHello 对照与本机混合握手成功 |

工具历史摘要只保存类型白名单和配对数量，不保存 call ID、工具名、参数、输出或加密内容。未配对数量表示“本次 payload 中未找到”，不等同于非法历史；`previous_response_id`、引用和压缩状态可能提供上下文。

## 传输实测

使用已复制的 App runtime `26.911.7940.0` 中 `codex 0.155.0-alpha.2.6`，配独立空 `CODEX_HOME`、虚拟鉴权和本机 TLS 模拟端点。外部代理只拒绝连接、不转发；未向官方账号发起请求。证据见 [TRANSPORT_BASELINE.json](TRANSPORT_BASELINE.json)，含二进制 SHA256，不含请求正文、认证值或本机用户名路径。

- supported groups：`0x11ec, 0x001d, 0x0017, 0x0018`，即 X25519MLKEM768 优先。
- 初始 key shares：`0x11ec`（1216 字节）、`0x001d`（32 字节）。
- HTTP ALPN：`h2, http/1.1`；WS 两次连接均没有 ALPN 扩展。
- HTTP/2 SETTINGS 顺序和值：`2=0, 4=2097152, 5=16384, 6=16384`；连接 WINDOW_UPDATE 为 `5177345`，与旧基线一致。
- 扩展顺序按连接变化，保留现有 rustls 排序实现，不固定为单次样本顺序。

HTTP 模拟请求完成；WS 模拟端点在 TLS 之后返回 400，此部分只验证握手，不作为 WS 官方服务端兼容性测试。自定义 provider 不带部分 Desktop/OAuth 专有头，其顺序继续依据 9 月 17 日的应用层抓包。

## 验证记录

- 配置、repository、TLS fingerprint 包完整测试通过。
- 新增及相关专项测试覆盖上述修复，全部在本地或模拟上游执行。
- `go test -tags=unit ./internal/service -json -count=1`：9563 条测试/子测试通过，3 条跳过；仍有 5 个既有失败测试函数，见下列名单。子测试包含在总数中。
- 普通服务包完整回归首次发现一个仍要求“不同任务共用固定 ID”的旧断言；已更新为任务隔离断言，并在上述 unit 全量中通过。
- 最终额度查询 Cookie 缓存隔离另跑相关专项及 QueryUsage/ResetCredit/PrepareUpstreamCall 回归，全部通过。
- `openai_codex_tool_frame.go`、`openai_codex_tool_frame_retry.go`、`ratelimit_service.go` 与修复前 SHA256 完全一致。其他文件中未修改重试次数、间隔、回退策略、额度阈值或冷却策略。

保留的既有失败：

1. `TestGetModelPricing_OpenAICompactAliasesFallback`
2. `TestComputeFinalAnthropicBeta_APIKeyHaiku_StillUsesAPIKeyBetas`
3. `TestComputeFinalCountTokensAnthropicBeta_OAuthTransparent_NoClientBetaInjectsDefault`
4. `TestBuildUpstreamRequest_APIKeyHaiku_RemainsUnmimicked`
5. `TestRateLimitService_HandleUpstreamError_OAuth401NoRefreshTokenIgnoredByDefault`

## 边界

- 不能据此认定任何差异导致了官方停用。之前六次工具配对 400 均没有 `previous_response_id`，本轮增量 ID 修复不能冒充这六次错误的根因修复；新增结构摘要用于后续定位。
- 未提供生命周期 metadata 的下游请求，无法可靠还原官方用户 turn、压缩次数或本地执行状态。非 UUIDv7 的任务种子仍依赖有界进程缓存，不保证跨重启生成同一个上游 UUID。
- Cookie 不会凭空生成；账号没有导入 Cookie 且上游未颁发时，请求仍可没有 Cookie。模型网关 Cookie 在内存中，不跨重启恢复；WebView 与模型网关未共用 Cookie 容器。WebView 仍会带入账号显式配置的 Cookie。
- “稳定任务会话”变更会改变原先启用全账号固定 session 的请求标识。本次发布后，旧任务的服务端关联和缓存可能受影响；不能声称无感迁移。Live 语音未纳入这次 Work Responses 生命周期修复。
- 09:23 发布版本只发送代理实际可测量的六项遥测，09:56 已另行扩充到 14 项，见 [遥测补齐说明](TELEMETRY_2026-09-18.md)；不伪造桌面插件、文件、沙箱、界面操作或实验曝光事件。2xx 日志仅表示采集端响应成功，不证明其后续采用该数据。
- 尚未做官方账号的长时间实测、全推理档位、完整子代理调度或启动辅助 API 的端到端时序验证；不添加模拟用户活动补齐这些空白。

## 线上发布与复查

- 发布标识：`e9f0b1f67-dirty-work-alignment-20260918T012023Z`；程序版本仍为 `0.1.165`，Linux amd64，前端已重新构建并嵌入。
- 新程序及运行进程 SHA256：`78f51461b3770c70b0201c38bb45b8bee212d968ff192a4e928892c216586667`。构建前后 3263 个源码文件校验一致。
- 09:26:42 复查（发布后 167 秒）：PID `3503347`、PM2 重启计数 `46` 保持稳定；内外网健康接口和 `/admin/accounts` 均为 HTTP 200，抽查的三个外网前端资源与本地构建字节一致；其他 PM2 服务的 PID、状态、重启次数未变。
- GPT-6 计价文件保持原 SHA256：`f125f18343d2f1e905b1d7213ad7fdb685b555f6f06109274fc3d73b6a8e38e3`。本次只替换并重启网关程序，未修改线上配置或账号状态。
- 发布后新增日志未见 panic、fatal、遥测配置错误、导出失败或序列上限告警。已有 `no available accounts` 告警仍在：发布前五分钟 120 条，发布后截至 09:26:23 为 73 条；两个统计窗口时长不同，不用于比较错误率。
- 观察期未出现 `Codex telemetry batch delivery` 记录，不能据此认定远端批次已送达。本次未额外发起官方模型或遥测探测请求。
- 旧程序备份：`/home/bald/sub2api-pool-bald/app/.deploy-tmp/sub2api.backup.20260918T012346Z.pre-c52c81f4153f`，SHA256 `c52c81f4153f07fe8471085f5e8387b621f027d581aa9c1d14c49d54945c6bf7`。发布脚本包含健康检查失败自动回滚。
- 线上发布回执：`/home/bald/sub2api-pool-bald/app/.deploy-tmp/deployment-e9f0b1f67-dirty-work-alignment-20260918T012023Z.json`。本地构建、发布及复查记录保存在 `.codex-official-desktop/2026-09-18/`。

账号未重新启用。部署成功不等于已经验证所有官方上游场景，也不代表解决了账号停用问题。
