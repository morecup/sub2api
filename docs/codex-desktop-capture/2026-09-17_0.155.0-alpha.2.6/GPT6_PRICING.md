# GPT-6 Astra 测试入口与计价

2026-09-17 补齐 `gpt-6-astra`：账号测试模型目录、OpenAI 白名单及预设映射、内置 JSON 价格表、动态目录缺失时的静态价格兜底。

价格核对来源：[OpenAI 模型页](https://developers.openai.com/api/docs/models/gpt-6-astra)、[API 定价页](https://developers.openai.com/api/docs/pricing)。以下均为美元 / 百万 token，表示基础模型价格；用户或渠道倍率仍按原配置计算。

| 模式 | 输入 | 输出 | 缓存读取 | 缓存写入 |
| --- | ---: | ---: | ---: | ---: |
| Standard | 10 | 50 | 1 | 12.50 |
| Fast / priority | 20 | 100 | 2 | 25 |
| Flex | 5 | 25 | 0.50 | 6.25 |

输入总量（包含缓存读取及写入）超过 272,000 token 时，整次请求的输入与缓存费率乘 2，输出费率乘 1.5；恰好达到阈值时仍使用短上下文价格。Fast 与 priority 在计费层统一识别。

保留动态价格源和渠道显式覆盖值，包括显式为零的缓存写入价格。Astra 的大小写、provider 前缀、推理后缀及 compact 计价变体使用 Astra 价格，避免落入其他 GPT 版本的默认价格。未新增未经官方确认的裸 `gpt-6` 别名。原有账号模型映射限制仍生效。

验证覆盖三个价格来源、模型变体、缓存费用、服务等级倍率、长上下文边界、动态价格合并和显式覆盖，以及 OAuth 测试请求实际携带的模型名。账号模型接口的 OAuth / API Key 回归通过；相关计价回归及前端 19 项测试通过。测试使用模拟上游，未新增付费推理请求。当前浏览器需要后台登录，未完成登录后的页面点击验证。

2026-09-17 23:44（Asia/Shanghai）已发布至 `107.175.76.239:18082`，发布标识为 `e9f0b1f67-dirty-gpt6pricing-20260917T154103Z`。线上进程哈希与构建一致，健康接口、首页及静态资源返回 200，模型选择资源包含 Astra，内置价格文件与上表一致。发布后约 96 秒复查无 panic、价格初始化失败或遥测导出告警。旧程序及内置价格文件均已备份，沿用上一轮默认遥测行为。
