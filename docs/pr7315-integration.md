# PR #7315 完整历史合并记录

本次将 Wei-Shaw/sub2api PR #7315 的备份提交 `3c2f05c957b4b93866318ec8695fc5a28fff70eb` 合入 morecup 分支，以 `98690cf41582b588af970394158b22b38887f7af` 为本地基线。保留完整父提交关系，包含备份分支上的 1,723 个上游提交和 PR 自身的 6 个提交。

## 已确认的合并选择

| 范围 | 合并后的行为 |
| --- | --- |
| GPT OAuth 画像 | 保留已有 Desktop 画像、请求头、UUID 派生、请求体元数据、Cookie、压缩、TLS 和遥测。 |
| 设备、会话、线程标识 | 继续由已有画像管理。OAuth 忽略上游 `codex_fingerprint_mode`，包括导入配置和重试上下文中残留的值；账号界面移除该选择。 |
| 新增凭据类型 | Setup Token 等新增路径保留上游实现，不让它们的身份改写覆盖既有 OAuth 画像。 |
| Grok | 保留已有 CLI 身份、请求头、遥测和重试策略；采用上游模型别名及主动查询后的界面刷新逻辑。 |
| Claude | 请求处理采用上游实现。 |
| 计价 | 已有及新增模型采用上游计价和长上下文规则，同时保留本地 Astra 别名对动态价格的正确解析。 |
| PR 打票功能 | 合入采集、缓存、配置和请求注入功能；票据在画像重建后注入，避免被重建过程清除。 |

打票的独立探测请求保持 PR 的专用流程、连接策略及打票代理配置，普通业务请求继续使用现有画像。

## 兼容处理

- HTTP、透传和 WebSocket 共用 OAuth 画像保护，头部、请求体和遥测继续使用同一身份来源。新加入的搜索 Responses 请求也使用该画像。
- 保留本地 Responses Lite 请求形态，接入上游工具兼容、错误分类及新增协议能力，并补齐对应回归检查。
- 新增 FedRAMP 认证标记在画像重建后保留，不改变普通账号的画像。
- 原有数据库迁移文件保持不变。`239_add_upstream_claude_monitor_template.sql` 单独补入上游的可选 Claude 监控模板，避免修改已部署的 `129` 迁移校验和；不覆盖已编辑的模板。
- 历史迁移的数字前缀可以重复；迁移程序按完整文件名记录与排序。
- 修复原分支已存在的 Antigravity 401 回归：缺少刷新令牌的账号直接标记鉴权错误，不再等待无法完成的自动刷新。GPT OAuth 的原有 401 配置行为保持不变。

## 抓包结论与验证边界

本地 2026-08-30 和 2026-09-17 的官方 Desktop 样本中，共核对 224 条模型请求，观察到的 session/thread 标识均相同。这些样本未覆盖子代理场景，不能据此断言所有官方客户端场景都必须相同。本次依照已验证的本地行为保留现有实现。

前端构建及全部 2,256 项测试通过。后端 `go test -tags=unit ./...` 全量通过：20,438 个测试项（含子用例）通过，17 项按环境条件跳过。普通后端构建和包含前端资源的 `embed` 构建均通过。

保留文件核对确认 16 项核心源码及画像基线与合并前一致，23 个 Claude 相关文件与备份上游一致。依赖真实数据库、Redis、外部 API、抓包或插件环境的部分测试未运行；未使用真实账号验证打票成功率，也未部署线上服务。
